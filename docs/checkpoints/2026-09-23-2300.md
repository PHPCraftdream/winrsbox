# Checkpoint — 2026-09-23 23:00

## Session summary

Продолжение после `/resume` чекпоинта `2026-09-23-1727`. Пользователь сообщил
о проблемах codex под песочницей (`winrsbox cx`): MCP-серверы `node_repl` и
`playwright` не стартуют (`handshaking ... connection closed: initialize
response`), позже — `invalid peer certificate: UnknownIssuer`. Оба исправлены
и подтверждены пользователем живьём; коммит `8b389c3` (не запушен).

Ключевые находки:
- state dir проекта `C:\Users\Computer\Desktop\pc` теперь
  `C:\Users\Computer\Desktop\.winrsbox\pc\` (старый `...\pc\.winrsbox\vx\` —
  заброшенный); долго смотрел не тот лог. Почему схема сменилась — не
  выяснено.
- MCP, корень: `inject.rs::patch_child_env_pairs` сравнивал
  `ProcessParameters.Length == 0x400`, реально 0x726 → патч env молча
  пропускался для ВСЕХ детей (FS_SANDBOX_SECTION и per-child ack name не
  доставлялись никогда). codex запускает MCP с явным урезанным env → хук без
  секции → fail-closed 0xC0000005. Фикс: `Length >= 0x400` + явный отсев WOW64.
- MCP, попутно: `NtAssignProcessToJobObject` блокировался безусловно
  (codex/process-wrap кладёт ребёнка в свой job). Теперь разрешено для своего
  ребёнка; опознание через `NtCompareObjects` с собственными дубликатами
  хэндлов (`proc/child_handles.rs`), т.к. хэндл codex = 0x901 без QUERY.
- Сертификаты: crypt32 открывает системные хранилища `NtCreateKey` с
  write-маской/MAXIMUM_ALLOWED; под Cow это был deny → пустой root store.
  Теперь Cow → read-only open-existing (`nt_create_key_effective_access`).
- Добавлены trace-логи (spawn_child flags/access, job_assign status/access,
  код выхода при terminate) и `winrsbox/scripts/dbgcap.py` (захват
  OutputDebugString без DebugView).

Воспроизведения лежат в `worktrees/jobprobe/` (gitignored; скрипты
jobprobe*.py, envbisect.py, paramlen.py) — можно удалить. Кронов нет.

## Active goal

none

## TaskList

### in_progress
(нет)

### pending
- #77 R04: закрыть после T1 + лёгкого T2 (blockedBy: #100)
- #97 R04-2b: замер UI-флагов — нужно разрешение пользователя
- #98 R04-2c (blockedBy: #97)
- #100 R04-4 документация (blockedBy: #98)
- #101 Overlay root publish path не канонизируется — не начинать без просьбы
- #107 Ложный HelloExeSpoof для git-bash `bin\..\usr\bin\bash.exe`
- #110 Производительность под sandbox + два процесса в одной папке

### recently completed
- #108 CX: MCP-серверы node_repl/playwright
- #109 CX: UnknownIssuer
- #102–#106 REG-1..REG-5

## Decisions

- Job-назначение разрешено только для своего ребёнка (по идентичности
  объекта), self/foreign — deny: вложенный job не снимает наших лимитов,
  breakaway у sandbox-job запрещён.
- Реестр под Cow: write-маска → read-only open существующего ключа вместо
  deny; Deny-префиксы (persistence) по-прежнему deny.
- Диагностические trace-строки оставлены (гейт trace), временные
  OutputDebugString-зонды в лаунчере удалены до коммита.

## Open questions

- #110: стало ли быстрее после фикса доставки ack (раньше дети могли ждать
  5 с таймаута ack)? Нужна ли параллельная работа двух winrsbox в одной папке
  (сейчас по коду второй упадёт на блокировке policy.redb — живьём не
  проверено)?
- Почему state dir сменился с `<project>\.winrsbox\vx` на
  `<parent>\.winrsbox\<basename>`.
- Пуш `8b389c3` — не запрошен.
- `npm/winrsbox/package-lock.json`, `dircheck.py`, `itemcheck.py` — untracked,
  не решено.

## Repo state

```
?? dircheck.py
?? itemcheck.py
?? npm/winrsbox/package-lock.json
```
```
8b389c3 fix(hook): codex MCP servers and TLS roots under the sandbox
9a47877 checkpoint: 2026-09-23-1727
282256b fix(hook): let relative opens against pipe/console/NUL handles through
aadaa4f fix: post-deploy regressions found live — MapView false positive, C: overlay migration gap, console corruption
bfcaac0 checkpoint: 2026-09-23-1317
```
