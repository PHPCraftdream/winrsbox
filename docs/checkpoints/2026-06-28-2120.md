# Checkpoint — 2026-06-28 21:20 [post-resume-npm-stall-suspected]

## Session summary

Это короткий пост-/resume checkpoint поверх `2026-06-28-2111.md` (свежий, 9 минут назад) — состояние практически идентично, но появился один новый ground-truth-факт, который стоит зафиксировать отдельно: фоновый Bash `bmh0x0wi9` (npm browser-tools install под песочницей, `repro/npm_fresh.log`) **не обновлял лог уже ~14 минут** (mtime 21:06:54, сейчас 21:20). Последние строки лога показывают характерный паттерн: `WARN spawn_failed status=0xc000012f` на `c:\users\computer\appdata\roaming\npm\npm.cmd` (STATUS_INVALID_IMAGE_NOT_MZ — npm.cmd это batch, не PE-image, ожидаемо), затем successful retry через `cmd.exe` → `node.exe`, после чего серия `[sandbox] hello from pid=22432 exe=...node.exe` без новых событий. Это либо npm молчаливо качает/распаковывает пакеты (легитимный quiet period npm install), либо реальный stall — неясно. Babysit cron `1a342412` тикает на 7/22/37/52, следующий тик через ~2 минуты должен пометить «still running #79» если mtime не обновится — и это уже будет сигнал «возможно завис».

В остальном — состояние совпадает с предыдущим checkpoint: 4 коммита этой сессии (`9ceaf20` whiteout-on-partial-delete, `018309e` hook-cache coherence, `3a5c8c8` whiteout cascade + sparse-overlay-dir defers, `4721b66` regression-ratchet) плюс ранее зафиксированный `181faa2` (variant B). Pipeline в самом дальнем прогоне фиксировал [OK] Repository ready → [OK] Virtual env → [OK] Main package installed (setuptools editable build прошёл = variant B подтверждён в полном пайплайне) → [OK] All dependencies installed → Installing Node.js dependencies (browser tools) и СЕЙЧАС там. TaskList не менялся: #79 in_progress, #72/#73/#80/#81 pending, остальные closed. Untracked в репо включает оба последних checkpoint'а (`2026-06-26-0030.md` появился до моей сессии, не моё; `2026-06-28-2111.md` — мой предыдущий) — git их не отслеживает по дизайну skill'а.

Архитектурная картина (актуально для следующей сессии): пользователь подвинул меня к более оптимистичной позиции по bindflt — для AI-агентского стека (CPython 3.11/3.12 + uv + git + node + npm + powershell) поверхность ФС-syscall'ов конечна, и стратегия «дожать Hermes user-mode + регрессионный храповик + coherence-фиксы» доминирует над bindflt-спайком до момента, когда реальная нелечимая user-mode кочка возникнет. Сейчас новых архитектурных решений в полёте нет.

Что в полёте: фоновый Bash `bmh0x0wi9` (mtime 14 минут назад — флажок), babysit cron `1a342412`. На @sxx не запущено. Я не делаю никаких commit'ов/push'ей/dep-bump'ов без явной просьбы; #80/#81 ждут пользовательского решения по стратегии (особенно #80 — там нужно архитектурное обсуждение, как guard различает image-backed vs private RWX mappings).

Если фоновый bash действительно завис — следующий шаг: TaskOutput на него, посмотреть полный хвост и BashOutput; решить — kill + анализ partial state, или дать ещё одну babysit-итерацию. Если завершится — открыть `repro/npm_fresh.log` целиком, искать `Installation complete` / exit code → закрыть #79 → разблокировать #72/#73 → переходить к #73 (запуск Hermes под песочницей и проверка работы).

## Active goal

Нет (`/goal` Stop hook не установлен).

## TaskList

### in_progress
- #79 Довести browser-tools npm install — последняя стадия Hermes (стратегия: orchestrator E2E сам, фоновый Bash `bmh0x0wi9`; mtime stale 14m — under investigation)

### pending
- #72 E2E установка Hermes в D:\ai_dev\hermes под песочницей — green (blockedBy: #79)
- #73 Запустить Hermes в песочнице и проверить рабочесть (blockedBy: #72)
- #80 --guard full ломает старт PowerShell/.NET (MapView PAGE_EXECUTE_WRITECOPY) — стратегия @sxx, требует обсуждения image-backed vs private RWX
- #81 os error 4395 reparse-point при удалении overlay-каталога (uv.lock sync) — стратегия @sxx

### recently completed
- #82 Regression-suite на закрытые overlay-faithfulness баги (храповик) — @sxx
- #78 Независимая верификация current working tree: Hermes E2E с current кодом #75
- #77 Диагностический проход: общий трейс для #75 + #76
- #76 SSH clone scaffold + Remove-Item не дочищает (whiteout-on-partial-delete фикс)
- #75 Per-process hook cache coherence (skip cache для Passthrough|Hidden)
- #74 Variant B: hybrid case-rewrite через OVERLAY_CASE
- #71 Enum-layer v2: rewrite кейса в NtQueryDirectoryFile (+NQDFEx)
- #70 Sanity-trace в hook_nt_query_directory_file для Python listdir

## Decisions

- **Не возрождаю работу заново после /resume** — TaskList уже совпадал со снэпшотом checkpoint'а; restoration не понадобилось. Это правильное поведение skill'а (resume идемпотентен, когда состояние уже на месте).
- **Не убиваю фоновый `bmh0x0wi9` сразу при stale-mtime в 14 минут** — npm install может легитимно молчать минутами при распаковке тяжёлых пакетов (puppeteer-class). Жду следующего babysit-тика (через ~2 минуты) или task-notification; если ещё через 15 минут mtime не двинется — TaskOutput + решение.
- **Не делаю action'ов по #80/#81 без обсуждения** — #80 особенно требует архитектурного решения (как hook distinguishes image-backed vs private RWX MapView) перед делегированием @sxx.

(Прочие материальные решения этой сессии — variant B, два-логических-коммита для #75/#78, отказ от non-wipe shortcut, регресс-ratchet через @sxx property-тесты — уже зафиксированы в `2026-06-28-2111.md` и здесь не дублируются.)

## Open questions

- **#79 stall vs busy** — npm_fresh.log mtime 21:06, сейчас 21:20. Реальный hang или quiet период npm install? Следующий babysit-тик (21:22) или ручной TaskOutput даст сигнал.
- **#80 strategy** — нужна архитектурная развилка перед делегированием @sxx (как различать image-backed vs private RWX MapView protection).
- **Install target `D:\ai_dev\hermes`** vs hard-coded `%LOCALAPPDATA%\hermes` в install.ps1 — нужен ли virtual-redirect политикой? (Перешло из прошлого checkpoint'а без изменений.)
- **Untracked шум** (`D…/`, `dev/`, `.githooks/`, `rename_*.txt`, и теперь два checkpoint'а в `docs/checkpoints/`) — gitignore или коммит? (Чекпойнты skill сознательно оставляет untracked.)
- **History hygiene перед push** — 26 коммитов ahead origin/master; `3bbe137/0454f1e` всё ещё с идентичными сообщениями.

## Repo state

```
 M .winrsbox/winrsbox/workdir/policy.redb
?? .githooks/
?? "D\357\200\272/"
?? docs/checkpoints/2026-06-26-0030.md
?? docs/checkpoints/2026-06-28-2111.md
?? repro/
?? winrsbox/rename_device_src.txt
?? winrsbox/rename_sibling_src.txt
```

```
4721b66 test(policy): regression ratchet for OVERLAY_CASE + git-object guard
3a5c8c8 fix(policy): cascade whiteout revival + don't mask real dirs with sparse overlay
018309e fix(hook): don't cache Passthrough/Hidden decisions (cross-process coherence)
9ceaf20 fix(hook): record whiteout on partial overlay delete (handle contention)
25c4b21 docs: checkpoint 2026-06-28-1200 + storage-engine research
```

Branch: `master`, 26 ahead `origin/master`, ни одного push'а. Babysit cron: `1a342412` (7,22,37,52 * * * *, session-only).
