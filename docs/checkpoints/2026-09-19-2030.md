# Checkpoint — 2026-09-19 20:30

## Session summary

Проведён полный аудит безопасности winrsbox (~48k строк Rust): 6 параллельных суб-агентов по зонам (hook core, dir_filter/IPC, proc/memory/token/system guards, reg/shell/com/alpc/ui guards, policy, ipc/launcher) плюс отдельная проверка npm-инсталлера. Все находки уровня critical/high дополнительно верифицированы построчным чтением кода оркестратором (нулевое доверие к отчётам суб-агентов).

Итог — **3 критические дыры контейнмента**:

1. `..`-обход: `compute()` решает по литеральной гостевой строке (`path_contained_in` — префикс-тест без фолдинга `..`, decide.rs:320-330), passthrough-рука (fs_hooks.rs:313-363) отдаёт путь ядру как есть → произвольная запись на реальный диск вне project_root.
2. `Req::RecordOverlay` без валидации (pipe_server.rs:1080-1083, decide.rs:369): гость задаёт произвольный real-disk путь назначения CoW; `prepare_overlay` (hooks.rs:1014-1041) не проверяет корень.
3. Секция `Local\WinRsBoxSession`: фиксированное имя, PAGE_READWRITE, дефолтный DACL (session_section.rs:57-67) → любой процесс сессии переписывает pipe_name/dll_path/disable_hooks → подмена канала решений и инъекция чужого DLL.

Плюс 9 high: env-килл-ключи `FS_SANDBOX_DISABLE_HOOKS`/`FS_SANDBOX_ALLOW_RWX` читаются из гостевого окружения (memory_guard.rs:1113/136); неза-hookленные сиблинги API — `NtAllocateVirtualMemoryEx`, `NtAlpcConnectPortEx`/`NtSecureConnectPort`, `ShellExecuteA`/`ExA`, win32u `NtUser*`, `DllGetClassObject`; спуф denylist спавна через `params->ImagePathName` вместо `PsAttributeImageName` (proc_guard.rs:883-899); token_guard fail-open при `owner_pid==0` (token_guard.rs:444/512); `STATUS_NO_MORE_FILES=0x104` — неверный код, скрытые записи возвращаются как успешные (dir_filter.rs:776); insert-only PID-гейт пайпа (реюз PID); write-passthrough на `%APPDATA%\Roaming\npm` с подменой hook.dll (sandbox.rs:116-119); async `NtQueryDirectoryFile` проходит нефильтрованным; class-48 маскинг мёртв для Path-1 layout (path_info_guard.rs:181-187). Ещё ~20 medium/low — в отчёте.

Полный отчёт с file:line, триггерами и порядком фиксов: `docs/audit-2026-09-19-security.md` (коммит 3fa701c). Изменений кода не вносилось — только аудит и документация. Live-эксплуатация не проводилась — все находки получены трассировкой кода.

## Active goal

Нет активного /goal. Следующий логичный шаг (пользователем не подтверждён): фикс находок в порядке из отчёта — (1) фолд `..` в `compute()`, (2) валидация RecordOverlay, (3) DACL/рандомное имя секции сессии, затем env-конфиг гвардии out-of-band и закрытие сиблинг-API.

## TaskList

Все задачи аудита закрыты (completed). Новых нет.

## Decisions

- Находки critical/high ре-верифицированы чтением кода; ограничение «только трассировка, без live-репро» явно отмечено в отчёте.
- Отчёт размещён в docs/ согласно структуре документации репо.

## Open questions

- Начинать ли фиксы и в каком порядке — ожидается решение пользователя.
- Незакоммиченная модификация `.gitignore` (3 вставки) — чужая, сознательно не тронута.

## Repo state

```
## master...origin/master [ahead 3]
 M .gitignore
```

```
3fa701c docs(audit): add 2026-09-19 security audit report
5c43a7c docs(review): sandbox security audit findings
3426bb6 checkpoint: 2026-09-19-1938
```
